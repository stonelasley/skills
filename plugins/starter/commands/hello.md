---
description: Confirm the st1-skills starter plugin is installed and working
---

Report that the `starter` plugin from the `st1-skills` marketplace is installed
and its commands are loading correctly.

If the user passed arguments, echo them back so they can confirm argument
passing works: $ARGUMENTS
